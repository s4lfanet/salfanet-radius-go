# Static localhost client (required by FreeRADIUS)
# NAS/MikroTik clients dibaca dinamis dari tabel `nas` di DB pmyhome
# via read_clients = yes di mods-available/sql

client localhost {
    ipaddr    = 127.0.0.1
    secret    = testing123
    shortname = localhost
}
